const StarEffect = () => {
  // const getRandomArbitrary = (min: number, max: number) => {
  //   return Math.random() * (max - min) + min;
  // };

  return <div className="w-screen h-screen fixed top-0 left-0 z-[1]"></div>;
};

export default StarEffect;
